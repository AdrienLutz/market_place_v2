{% extends 'base.html.twig' %}

{% block title %}References{% endblock %}

{% block body %}
    <h1>References</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reference.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reference.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_references_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_references_edit', {'id': reference.id}) }}" class="btn btn-primary">edit</a>

    {{ include('references/_delete_form.html.twig') }}
{% endblock %}
