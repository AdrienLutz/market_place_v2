{% extends 'base.html.twig' %}

{% block title %}Distributeurs{% endblock %}

{% block body %}
    <h1>Distributeurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ distributeur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ distributeur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_distributeurs_index') }}">back to list</a>

    <a href="{{ path('app_distributeurs_edit', {'id': distributeur.id}) }}">edit</a>

    {{ include('distributeurs/_delete_form.html.twig') }}
{% endblock %}
